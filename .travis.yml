language: bash

before_install:
  - sudo apt-get update && sudo apt-get install -y shellcheck

script:
  - ls -l
  - bash examples/test.sh
  - ls -l  
  - cat examples/file 

notifications:
   email:
      on_success: never
